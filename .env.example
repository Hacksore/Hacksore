# Cloudflare R2 Configuration
# Required: Bucket name for your R2 bucket
R2_BUCKET_NAME=your-bucket-name

# Required: Your Cloudflare account ID (found in R2 dashboard)
R2_ACCOUNT_ID=your-account-id

# Required: R2 API token access key ID
R2_ACCESS_KEY_ID=your-access-key-id

# Required: R2 API token secret access key
R2_SECRET_ACCESS_KEY=your-secret-access-key

# Optional: Custom R2 endpoint URL (auto-constructed from account ID if not provided)
# Format: https://<account-id>.r2.cloudflarestorage.com
# R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# Optional: Prefix to filter objects in the bucket (e.g., "images/" or "photos/")
# R2_PREFIX=

# Optional: Custom public domain URL for your R2 bucket (if you have a custom domain configured)
# This will be used instead of the default R2 public URL
# R2_PUBLIC_URL=https://your-custom-domain.com

# Optional: Set to "true" to use presigned URLs for private buckets
# If false or unset, will use public URLs (requires public bucket or custom domain)
# R2_USE_PRESIGNED_URLS=false
